"use client";

// Early Access Pill is now replaced by FullScreenPresaleModal in presale-gate
// This component is kept for backward compatibility but doesn't render anything

export function EarlyAccessPill() {
  return null;
}
