"use client";

// Full-screen presale modal removed. Stub kept to satisfy any lingering imports.
interface PresaleModalProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function FullScreenPresaleModal({ onOpenChange }: PresaleModalProps) {
  return null;
}
